include ../base-dropdown/base-dropdown

mixin rooms-dropdown(props={})
  - const { isOpen=false, windowListener=true, inputListener=true, buttons=true, items=false, template=false } = props

  div(data-buttons=buttons, data-items=items, data-template=template).rooms-dropdown.js-roomsDropdown
    +baseDropdown({ isOpen, windowListener, inputListener })
      block